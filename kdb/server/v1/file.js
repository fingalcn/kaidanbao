/**
 * 1,加载静态资源文件(只能读取resourse内的文件)
 * 2,文件上传
 */
var log = require('./log');
var fs = require('fs');
var cache={};

//读文件
function read(fileName,comp){
}
exports.read = read;
// module.exports.printNextCount = printNextCount;
